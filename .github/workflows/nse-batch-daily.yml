name: NSE Daily Batch Analysis

on:
  schedule:
    # Run at 2:45 PM IST (9:15 AM UTC) on weekdays only
    - cron: '15 9 * * 1-5'
  workflow_dispatch: # Allow manual trigger for testing

env:
  RUST_LOG: info
  CARGO_TERM_COLOR: always

jobs:
  nse-batch-analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 10  # Kill entire job after 10 minutes
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable
        
    - name: Cache Cargo registry
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target/
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-
          
    - name: Build NSE Analyzer
      run: cargo build --release
      
    - name: Run NSE Batch Analysis
      timeout-minutes: 3  # Kill this step after 3 minutes (180 seconds)
      run: |
        # Set environment for batch mode only
        export NSE_MODE=batch
        
        # Run the batch analysis
        ./target/release/nse-analyzer
        
        # Create summary data for email body (not as file)
        if [ -f "batch_rules.json" ]; then
          echo "ANALYSIS_SUCCESS=true" >> $GITHUB_ENV
          
          # Count total alerts and securities
          ALERT_COUNT=$(jq '[.[].alerts | length] | add // 0' batch_rules.json)
          SECURITY_COUNT=$(jq 'length' batch_rules.json)
          
          echo "ALERT_COUNT=$ALERT_COUNT" >> $GITHUB_ENV
          echo "SECURITY_COUNT=$SECURITY_COUNT" >> $GITHUB_ENV
          
          # Get securities with alerts (names)
          SECURITIES_WITH_ALERTS=$(jq -r '.[].symbol' batch_rules.json | tr '\n' ', ' | sed 's/,$//')
          echo "SECURITIES_WITH_ALERTS=$SECURITIES_WITH_ALERTS" >> $GITHUB_ENV
          
          # Create alert type breakdown with security names
          echo "Creating alert breakdown table..."
          echo "Debug: batch_rules.json content preview:"
          jq -c '.[0:2]' batch_rules.json || echo "Failed to parse JSON"
          
          # Create table data for each alert type using a more robust approach
          echo "ALERT_TABLE_DATA<<EOF" >> $GITHUB_ENV
          
          # Get all alert types and their securities in one go
          jq -r '
            # Group all alerts by type
            [.[].alerts[] | {type: .alert_type, symbol: .symbol}] | 
            group_by(.type) | 
            map({
              alert_type: .[0].type, 
              count: length, 
              securities: [.[].symbol] | unique | join(",")
            }) | 
            sort_by(.count) | reverse | 
            .[] | 
            "\(.alert_type)|\(.count)|\(.securities)"
          ' batch_rules.json | tee /tmp/debug_table.txt
          
          echo "EOF" >> $GITHUB_ENV
          
          echo "Debug: Generated table data:"
          cat /tmp/debug_table.txt
          
        else
          echo "ANALYSIS_SUCCESS=false" >> $GITHUB_ENV
          echo "ALERT_COUNT=0" >> $GITHUB_ENV
          echo "SECURITY_COUNT=0" >> $GITHUB_ENV
          echo "SECURITIES_WITH_ALERTS=None" >> $GITHUB_ENV
          echo "ALERT_TABLE_DATA=" >> $GITHUB_ENV
        fi
        
    - name: Archive analysis results
      if: env.ANALYSIS_SUCCESS == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: nse-batch-rules-${{ github.run_number }}
        path: batch_rules.json
        retention-days: 30
        
    - name: Format Alert Table
      id: format-table
      if: always()  # Run even if analysis failed
      run: |
        echo "Debug: ANALYSIS_SUCCESS = $ANALYSIS_SUCCESS"
        echo "Debug: ALERT_TABLE_DATA (raw):"
        printf '%s\n' "$ALERT_TABLE_DATA"

        if [ "$ANALYSIS_SUCCESS" = "true" ] && [ -n "$ALERT_TABLE_DATA" ]; then
          echo "Using ALERT_TABLE_DATA from env..."
          formatted_output="$ALERT_TABLE_DATA"
        else
          if [ "$ANALYSIS_SUCCESS" = "true" ]; then
            formatted_output="No alerts found - all securities clean!"
          else
            formatted_output="Analysis failed or timed out - no data available"
          fi
        fi

        {
          echo "formatted_table<<EOF"
          echo "$formatted_output"
          echo "EOF"
        } >> "$GITHUB_OUTPUT"

    - name: Generate IST Timestamp
      run: |
        IST_TIME=$(TZ="Asia/Kolkata" date +"%d %b %y %H:%M")
        echo "IST_TIMESTAMP=$IST_TIME" >> $GITHUB_ENV
        
    - name: Send email notification
      if: always()  # Send email even if analysis fails
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com
        server_port: 587
        username: ${{ secrets.GMAIL_USERNAME }}
        password: ${{ secrets.GMAIL_PASSWORD }}
        subject: "NSE Analysis Report - ${{ env.IST_TIMESTAMP }} (${{ env.ANALYSIS_SUCCESS == 'true' && 'SUCCESS' || 'FAILED' }})"
        to: ${{ secrets.EMAIL_RECIPIENT }},${{ secrets.EMAIL_RECIPIENT_2 }}
        from: ${{ secrets.GMAIL_USERNAME }}
        body: |
          NSE Daily Analysis Report
          =============================================
          
          📊 SUMMARY
          • Securities with alerts: ${{ env.SECURITY_COUNT || '0' }}
          • Total alerts generated: ${{ env.ALERT_COUNT || '0' }}
          • Status: ${{ env.ANALYSIS_SUCCESS == 'true' && '✅ Completed Successfully' || '❌ Failed/Timeout' }}
          
          🏢 SECURITIES WITH ALERTS
          ${{ env.SECURITIES_WITH_ALERTS || 'None' }}
          
          📈 ALERT BREAKDOWN (AlertType|Count|Securities)
          ===============================================
          ${{ steps.format-table.outputs.formatted_table }}
          
          ${{ env.ANALYSIS_SUCCESS != 'true' && 'ℹ️  TROUBLESHOOTING
          • NSE API timeout (90 second limit)
          • Network connectivity issues  
          • NSE server problems
          • Market hours/trading session status' || '' }}
          
          🔗 ADDITIONAL INFO
          • Run ID: ${{ github.run_number }}
          • Repository: ${{ github.repository }}
          • Full artifacts: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          • Generated at: ${{ env.IST_TIMESTAMP }}
        attachments: batch_rules.json
        
    - name: Notification on failure
      if: failure()
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com
        server_port: 587
        username: ${{ secrets.GMAIL_USERNAME }}
        password: ${{ secrets.GMAIL_PASSWORD }}
        subject: "🚨 NSE Analysis FAILED - ${{ env.IST_TIMESTAMP }}"
        to: ${{ secrets.EMAIL_RECIPIENT }}
        from: ${{ secrets.GMAIL_USERNAME }}
        body: |
          ⚠️ NSE Daily Analysis Failed
          
          Run ID: ${{ github.run_number }}
          Repository: ${{ github.repository }}
          Generated at: ${{ env.IST_TIMESTAMP }}
          
          Please check the logs at:
          ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          
          Error details may be available in the workflow logs.